meta: ~

director_uuid: (( meta.director_uuid ))

compilation:
  cloud_properties:
    name: random
  network: redis_z1
  reuse_compilation_vms: true
  workers: 2

networks: (( merge ))

jobs: (( merge ))

resource_pools: ~

properties: (( merge ))
