meta: ~

director_uuid: (( merge ))

compilation:
  cloud_properties:
    name: random
  network: redis_z1
  reuse_compilation_vms: true
  workers: 2

networks: (( merge ))

resource_pools:
- cloud_properties:
    ram: 1024
    name: random
  name: redis_z1
  network: redis_z1
  stemcell:
    name: bosh-warden-boshlite-ubuntu-trusty-go_agent
    version: latest

properties:
  template_only: (( merge ))
  broker:
    name: (( merge ))
